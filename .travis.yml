language: node_js
node_js:
    - "lts/*"
cache:
    directories:
        - "node_modules"
        - "build"
script:
    - yarn
    - yarn setup
    - yarn build:docs
    - yarn lint
    - yarn typecheck
    - yarn test:ci
branch: master
