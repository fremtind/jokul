---
"@fremtind/jokul": patch
---

Sørger for at ikke `RadioPanel` og `CheckboxPanel` med korte valgtekster bryter over flere linjer. Legger også til et eksempel for `RadioPanel` som viser bruk med korte tekster.
