import React from "react";
import { Icon } from "../Icon";
import { type IconProps } from "../types";

export const ChevronLeftIcon = (props: IconProps) => <Icon {...props}>keyboard_arrow_left</Icon>;
