---
title: useElementDimensions
react: react-hooks
group: hooks
---

import {
    ElementDimensionsExample,
    elementDimensionsExampleCode,
} from "./ElementDimensionsExample";

<Ingress>
    Du kan bruke useElementDimensions for å finne dimensjonene til et element på
    siden. Du kan også følge med på endringer i størrelse når brukeren endrer
    størrelse på nettleservinduet.
</Ingress>

<ComponentExample
    title="useElementDimensions"
    component={ElementDimensionsExample}
    codeExample={elementDimensionsExampleCode}
/>

## Bruk

useElementDimensions returnerer en `ref` som du kan sette på elementet du vil finne størrelsen til, sammen med et objekt med dimensjonene (`height` og `width`) i piksler. Begge deler returneres i et array, så du kan selv gi navn til verdiene:

```tsx
const [myElementRef, myDimensions] = useElementDimensions<HTMLDivElement>();
```

Hooken lytter også etter forandringer når nettleservinduet endrer størrelse. Av ytelsesgrunner begrenser hooken hvor often størrelsen leses til én gang hver 200 millisekunder. Du kan endre hvor hyppig lesingen skal skje når du kaller hooken dersom du trenger det.

```tsx
const [ref, dimensions] = useElementDimensions(400);
```

Vær forsiktig med å sette et for lavt delay, eller lytte etter for mange størrelser på denne måten, da det kan fyres av veldig mange rerenders på kort tid ved å gjøre dette.
