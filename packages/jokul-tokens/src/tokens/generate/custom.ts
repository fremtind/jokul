import { generatePalette } from "../../colors/colors.js";
import {
    type ColorScheme,
    type CustomPalette,
    GLOBAL_COLORS,
    type HexColor,
} from "../../colors/types.js";

export function generateCssColorTokens(
    paletteOrHexColor: CustomPalette | HexColor,
) {}
