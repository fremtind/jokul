//import pkg from "../package.json";
//import { e2eTest } from "../../../jest/e2e-a11y-util";

//e2eTest({ port: 1111, packageName: pkg.name.slice(14) });
describe("noop", () => {
    test("should use storybook to do visual regression testing", () => {
        expect(true).toBeTruthy();
    });
});
