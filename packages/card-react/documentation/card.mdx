---
title: Card
react: card-react
scss: card
group: layout
keywords: navcard taskcard infocard
---

import { NavCardExample, navCardExampleCode } from "./NavCardExample";
import { FakturainfoExample, fakturainfoExampleCode, fakturainfoExampleProps } from "./FakturainfoExample";
import { StatuskortExample, statuskortExampleCode, statuskortExampleProps } from "./StatuskortExample";
import { TaskCardExample, taskCardExampleCode } from "./TaskCardExample";
import { InfoCardExample, infoCardExampleCode } from "./InfoCardExample";
import { TopExample, topExampleCode } from "./TopExample";
import {
    baseCardExampleProps,
    navCardExampleProps,
    infoCardExampleProps,
    taskCardExampleProps,
} from "./cardExampleProps";

<Ingress>Kort lar oss løfte frem oppgaver eller innhold som henger sammen.</Ingress>

<ComponentExample
    title="Informasjon om forfalt faktura"
    component={FakturainfoExample}
    codeExample={fakturainfoExampleCode}
    knobs={fakturainfoExampleProps}
/>

Som hovedsak brukes kort for å vise innhold som hører tett sammen, og eventuelle handlinger som hører til innholdet. Plasser elementer i kortet på en måte som er enkel å skumme for nyttig informasjon, og som gir et tydelig hierarki.

Kortkomponenten setter kun noen få egenskaper og stiler, og det er opp til deg å bygge innholdet i kortet ut fra behov og informasjon.

Hele kortet kan være **klikkbart**. Dette indikeres med at kortet får skygge, og en effekt ved hover og fokus. Ikke plasser andre interaktive elementer i et kort som er klikkbart.

## CardImage

I pakken finnes også en `CardImage`-komponent som gjør det enkelt å legge inn bilder som blør ut i kantene av kortet, selv om man har padding på resten av innholdet. Komponenten rendres i utgamgspunktet som et vanlig `<img>`-element, men du kan også sende inn en egen komponent gjennom `as`-propen. Dermed kan du for eksempel bruke `Image`-komponenten fra Jøkul, eller tilsvarende komponenter fra et rammeverk eller CMS, for å håndtere bilder mer avansert.

<ComponentExample
    title="Statuskort for forsikring med bilde"
    component={StatuskortExample}
    codeExample={statuskortExampleCode}
    knobs={statuskortExampleProps}
/>

## Navigasjonskort

> Du ender opp på riktig sted

<ComponentExample
    title="NavCard"
    component={NavCardExample}
    knobs={navCardExampleProps}
    codeExample={navCardExampleCode}
/>

Navigasjonskort er klikkbare, og tar brukeren videre til et annet skjermbilde i løsningen. Kortet inneholder alltid en tittel i form av en `NavLink`, og kan i tillegg ha bilde, undertittel, tag, og ekstra informasjon. Siden hele kortet er klikkbart skal det _ikke_ inneholde interaktive elementer.

## Oppgavekort

> Du får gjort det du skulle

<ComponentExample
    title="TaskCard"
    component={TaskCardExample}
    knobs={taskCardExampleProps}
    codeExample={taskCardExampleCode}
/>

I oppgavekortet kan du samle handlinger som hører sammen med annen informasjon. Eksempler på dette kan være valg for tilpasning av en tilleggsdekning, muligheter for videre behandling av en åpen skadesak, eller visning av forsikringsdetaljer med mulighet for endring.

Du kan velge mellom noen fastsatte bakgrunner, og velge å fremheve kortet med skygge, ellers bestemmer du selv innholdet i kortet. Siden kortet i seg selv ikke er klikkbart kan du bruke interaktivt innhold som lenker, knapper og skjemaelementer.

## Infokort

> Du finner det du lurer på

<ComponentExample
    title="InfoCard"
    component={InfoCardExample}
    knobs={infoCardExampleProps}
    codeExample={infoCardExampleCode}
/>

Infokortet brukes til ren informasjon. Det kan ha et bilde, og bør ellers inneholde en tittel sammen med eventuell ekstra informasjon. Kortet skal ikke ha knapper eller lenker, og du setter selv en bakgrunnsfarge som passer til bruken i din løsning.
